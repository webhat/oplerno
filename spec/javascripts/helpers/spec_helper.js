// Hackerty, hack. Stop Jasmine from failing with
Authy = {UI: {instance: {setTooltip: function (a, b) {
}}}};

beforeEach(function () {
   // jasmine.getEnv().addReporter(new jasmine.TrivialReporter());
   // jasmine.getEnv().execute();
});