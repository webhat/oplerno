
<tr>
  <td width="100" valign="top"></td>
  <td width="500"><p>Hello <%= @resource.email %>!</p></td>
</tr>
<tr>
  <td width="100" valign="top"></td>
  <td width="500"><p>Someone requested a link to change your password. You can do this through the link below.</p>

    <p><b><%= link_to 'Change my password', edit_password_url(@resource, :reset_password_token => @token), {style: 'color: orange; text-decoration: none;'} %></b></p>

    <p>If you didn't request this, please ignore this email.</p>
    <p>Your password won't change until you access the link above and create a new one.</p>

    <p><%= t('devise.mailer.broken_link') %></p>
    <p><%= edit_password_url(@resource, :reset_password_token => @token) %></p></td>
</tr>



